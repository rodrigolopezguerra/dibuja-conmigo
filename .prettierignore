dist
node_modules

# index.html is relocated verbatim (legacy markup, byte-for-byte structure)
# and now serves the live site directly from the repo root; never
# auto-reformatted.
/index.html

# styles/*.css (excluding the test) are relocated verbatim from the legacy
# inline <style> block (design invariant I5: pure cut-and-paste, no
# reformatting) — never auto-reformatted, same rationale as index.html above.
styles/tokens.css
styles/base.css
styles/gallery.css
styles/board.css
styles/toolbar.css
